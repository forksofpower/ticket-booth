{
  "name": "ticket-booth",
  "packageManager": "yarn@3.6.1",
  "workspaces": [
    "services/*",
    "lib/*"
  ],
  "dependencies": {
    "@yarnpkg/pnpify": "^4.0.0-rc.48"
  },
  "scripts": {
    "format": "npx prettier -w \"./**/*.{js,jsx,ts,tsx}\" --log-level=warn",
    "test:all": "yarn workspaces foreach run test --colors",
    "publish:common": "yarn workspace @forksofpower/ticketbooth-common run pub && yarn install && git add . && git commit -m 'chore: publish common'",
    "publish:tsconfig": "yarn workspace @ticketbooth/tsconfig run pub && yarn install && git add . && git commit -m 'chore: publish tsconfig'"
  },
  "devDependencies": {
    "prettier": "^3.0.3"
  },
  "prettier": "@ticketbooth/configs/prettier-config"
}
