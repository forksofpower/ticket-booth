{
  "name": "ticket-booth",
  "packageManager": "yarn@3.6.1",
  "workspaces": [
    "services/*",
    "lib/*"
  ],
  "dependencies": {
    "@yarnpkg/pnpify": "^4.0.0-rc.48"
  },
  "scripts": {
    "common:publish": "yarn workspace @forksofpower/ticketbooth-common run pub && yarn install && git add . && git commit -m 'chore: publish common'"
  }
}
